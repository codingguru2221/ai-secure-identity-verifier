## Packages
react-dropzone | High quality drag and drop file upload component
framer-motion | Smooth animations for the verification scanning effect and reveals
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility to merge tailwind classes without style conflicts
@radix-ui/react-progress | Required for the risk score visualization
@radix-ui/react-tooltip | Required for helpful tooltips on technical terms

## Notes
- The `/api/verify` endpoint expects a `multipart/form-data` payload with a `file` field.
- The frontend will handle the file selection and wrap it in FormData before sending.
- Using a dark, modern cybersecurity aesthetic with monospaced accents.
